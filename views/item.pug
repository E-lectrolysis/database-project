doctype html
html
  head
    meta(charset="utf-8")
    title
    meta(name="author" content="")
    meta(name="description" content="")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(href="/css/style.css" rel="stylesheet")
  body
    h1 #{item.item} 
    p #{item.description}
    h2 Acquisition
    div.loot-table
        ul
            each entry in drops 
                li
                    a(href="/monsters/" + entry.monster) #{entry.monster}